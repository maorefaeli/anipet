@{
    ViewBag.Title = "Index";
    anipet.Models.User currentUser = (anipet.Models.User)Session["User"];
}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        #
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="/Home/Index">Home <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/Home/About">About</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/Products/Index">Products</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/Purchases/Index">Login</a>
                </li>
            </ul>
        </div>
    </nav>
<div class="box" style="flex-direction: column">
    <div class="col-lg-12">
        <hr>
        <h2 class="intro-text text-center text-danger" style="font-size: 30px">
            Error
            <strong>@ViewBag.ErrorMessage</strong>
        </h2>
        <hr>
    </div>
    <div style="text-align: center; font-size:30px;">
        @if (currentUser == null)
        {
            @Html.ActionLink("Login here", "Login", "Users")
        }
    </div>
</div>
