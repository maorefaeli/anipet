@model anipet.Models.FoodSource
@{
    anipet.Models.User currentUser = (anipet.Models.User)Session["User"];
    ViewBag.Title = "Food Source Details";
}

<div class="box" style="flex-direction: column"; align-items: center;>
    <hr>
    <h2 class="intro-text text-center">
        <strong>Food Source</strong>
        <small>details</small>
    </h2>
    <hr>
    <div>
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Name)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Name)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.SourcePricePerKilo)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.SourcePricePerKilo)
            </dd>

        </dl>
    </div>
    <p style="margin-left: 900px;">
        @if (currentUser != null && currentUser.IsAdmin)
        {
            @Html.ActionLink("Edit", "Edit", new { id = Model.Id })
            <text>|</text>
        }
        @Html.ActionLink("Back to List", "Index")
    </p>
</div>